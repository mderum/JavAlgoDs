  //reverse a string 
    
    String x="erum";
    
    for(int i=x.length()-1; i>=0; i--){
      System.out.print(x.charAt(i));
    }
    
    System.out.println("------");
    
       for(int i=0; i<=x.length()-1; i++){
      System.out.print(x.charAt(x.length()-1-i));
    }
        System.out.println("------");


        System.out.println(   Arrays.stream( x.split("") ).reduce( "", 
     (s,c) -> c+s , 
     (s1,s2) -> s2+s1
    ));

 
        // count chars in a given string 

    System.out.println(    Arrays.stream( "eeevmo o m".split("")).
    collect(Collectors.groupingBy( z -> z ,  Collectors.counting()) ));
